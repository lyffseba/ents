fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
stripe>=10.0
sqlalchemy>=2.0
apscheduler>=3.10
python-dotenv>=1.0
pydantic>=2.7
httpx>=0.27
# Note: google-generativeai is deprecated; prefer google-genai (new unified) or google-cloud-aiplatform (Vertex).
# Current code uses generativeai for immediate functionality + Gemini call compliance.
# When switching: update deps.py call_gemini and config.
google-generativeai>=0.7
# google-genai>=0.1
# google-cloud-aiplatform>=1.50  # Best for "Google Cloud" + Vertex Gemini
# Optional System 1 local backend (demo and CI use SYSTEM1_BACKEND=mock):
#   pip install laya
# Public weights: https://huggingface.co/convaiinnovations/laya
# The adapter is web/system1/laya.py (laya.Router.predict, not chat completions).
# First English predict downloads ~800MB unless SYSTEM1_LAYA_PATH is a local
# checkpoint directory. Missing package or weights fall back to the mock.
# Offline check: python -m web.system1.laya_live --fixture
# Live weights:  python -m web.system1.laya_live
